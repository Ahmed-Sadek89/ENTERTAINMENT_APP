// styles
import { MainHeader } from "./Styles";

const Header = () => {
    return (
      <MainHeader onClick={() => window.scroll(0,0)}>
        🎬 Entertainment Hub 🎥 
      </MainHeader>
    );
  }
  
  export default Header;
  